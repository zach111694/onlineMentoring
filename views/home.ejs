<!DOCTYPE html>
<html lang="en">

<%- include headIncludes.ejs %>

<body>

    <div id="wrapper">

       <%- include sidebarInclude.ejs %>

        <!-- Page Content -->
        <div id="page-content-wrapper">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <h1>Welcome <%= user.first_name %>!</h1>
                        <br>
                        
                        <% if(user.survey == "no") {%>
                            <h4>It looks like you haven't built your profile. Take the survey below in order to ssget on the pairing queue.</h4>
                            <br>
                            <a class="btn btn-primary" href="/survey" role="button">Survey</a>
                        <% } else { %>
                            <% if(user.paired == ""){ %>
                                <h4>Please wait until a good match for you is found</h4>
                            <%} else {%>
                                <h4>A great match has been found!</h4>
                                <h4>You have been paired with <%= user.paired %> for a score of <%= user.score %> out of 4</h4>
                                <br>
                            <%}%>
                        <%}%>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-content-wrapper -->

    </div>
    <!-- /#wrapper -->

    <%- include jsIncludes.ejs %>
    
</body>

</html>
